name: First workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run : echo "Hello world!"
      - name: Print good bye
        run: echo "Done! bype!"
      - name: Print multiline text
        run: |
              echo Hello world
              echo good bye mulitline
